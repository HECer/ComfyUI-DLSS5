# DLSS usability and reliability implementation

The user approved all seven recommendations from the read-only 0.4.0 / 9d1c34b review and explicitly requested a real Yoke loop with routing and code intelligence. This is plan approval; do not ask again. Implement locally, test and commit through Yoke; no push, publication or deployment.

Keep every existing node ID, required input name/order, existing dropdown value/order, output order, default and native rendering setting compatible. New optional fields may be appended with safe defaults. No runtime DLL changes, no dependency upgrades, no replacement of ComfyUI's Torch environment. Preserve originals and existing custom configuration. Do not remove AI disclosure/provenance.

Scope: repeatable BOM-compatible configuration and setup; optional Frame Generation installation; truthful staged runtime diagnostics and early preflight; descriptions/tooltips/categories and meaningful operation hints; bounded Easy single-stage processing and truthful reports; progress, native cancellation and advisory memory estimates; concise first-run documentation and integration regression coverage.

Do not silently replace DA-V2 with VDA, change numerical presets, introduce scene-cut resets into SR/NR, or redesign model cache ownership. Those require separate image-quality experiments. Preserve persistent-mode numerical behavior. Test bounded single-stage processing with deterministic identity/frame-index fakes for chronology, overlap and exact length, plus native GPU checks when available. A passing mock is not proof of image quality.

Use .yoke/prd.yaml as acceptance authority. Tests must be meaningful behavioral regressions and must not download models or modify installed runtimes. Existing tests remain intact except legitimate contract updates such as richer status wording. Add new tests in tests/test_usability.py, using the criterion ID in test names so each targeted gate runs real tests. No empty suites or marker-only assertions. For UI/doc changes, prioritize semantic node-schema compatibility and executable workflow checks over string-only tests. Capture before/after node schemas in a fixture for compatibility.

Environment: Windows PowerShell, rtk required. The default Python already provides Torch/pytest. Runtime DLLs are tracked with Git LFS. The global Yoke 1.14.0 fingerprint refuses files above 64 MiB; .yoke/tooling/yoke is a local copy which streams every byte into the same SHA-256 instead of imposing the size cap, uses actual installed Graphify version and newline MCP framing, and unwraps Serena structured results. Global installation and DLLs untouched. Use node .yoke/tooling/yoke/dist/cli.js for the supervised loop. No guard is bypassed.

Code intelligence: active locally patched Yoke facade. Graphify, pinned Graft and pinned Serena are configured in .yoke/config.yaml and have passed a real code_context/code_symbol probe. Request semantic context and inspect provenance/warnings; use bounded AST/rg fallback when a backend fails. Graphify extraction is local code-only. Do not claim complete references solely from a backend coverage label.

Review all stories independently in Yoke. Run full tests at each gate. At completion inspect saved workflows, PowerShell parse checks and feasible GPU/UI smoke tests. Record exact limitations in final handoff.

Native regression baseline is outside this git root in ../test-output/yoke-native-regression. Use ../test-env/Scripts/python.exe for those tests: it has NumPy and VapourSynth. The existing configured VapourKit Python initially lacked NumPy. The coordinator installed only that missing package (2.5.2) into this isolated bridge interpreter; real SR, NR and persistent combined outputs now match the baseline exactly (max absolute delta 0). DLSS-2 fixes installer dependency handling and DLSS-4 must diagnose missing dependencies for other installations. Do not modify installed runtimes from automated unit tests.

The outer Yoke supervisor performs the independent read-only review with a different Codex model after implementation. Workers must not spawn inner agents or request another review; finish focused tests and return promptly.

Windows test environment: pytest temporary-directory creation has produced WinError 5 under the worker sandbox. Do not repeat the same failure or create multiple alternate temp locations. Use the normal tool approval mechanism for the exact test command if available; its automatic review approved these test runs in DLSS-1. If execution remains unavailable, report it truthfully and return: the outer supervisor independently executes criterion gates and the full suite, bound to the exact source fingerprint. A read-only reviewer receives actual supervisor test output in its prompt and must assess code/test quality without attempting write-requiring pytest itself.

Routing checkpoint after DLSS-2: measured SOL-high serial implementation/review turns were slow. Remaining native/diagnostic implementation stories DLSS-4/5/6 route directly to the frontier profile; node-help/docs stay on standard with frontier escalation. Independent reviews remain separate fresh model invocations; Astra reviews SOL/Terra implementations at high reasoning, SOL reviews Astra implementations at medium reasoning. All criteria, full-suite gates, review verdict checks, fingerprint guards, isolation and repair gates remain unchanged.

Tool efficiency: print the entire exec_command result, including session_id when a command is still running. Poll that exact session with write_stdin until completion; do not treat a yielded process as a failed test or rerun the same suite in many partitions. Run relevant failing regressions, then the full suite once after the final fix. The outer supervisor performs the separate required gates.
