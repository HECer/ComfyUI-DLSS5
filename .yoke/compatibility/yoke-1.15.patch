diff --git a/dist/workspace/fingerprint.js b/dist/workspace/fingerprint.js
index dfaead1..6562e2c 100644
--- a/dist/workspace/fingerprint.js
+++ b/dist/workspace/fingerprint.js
@@ -1,6 +1,6 @@
 import { createHash } from 'node:crypto';
 import { execFileSync } from 'node:child_process';
-import { lstatSync, readFileSync, readdirSync, readlinkSync, realpathSync } from 'node:fs';
+import { lstatSync, openSync, readSync, closeSync, readFileSync, readdirSync, readlinkSync, realpathSync } from 'node:fs';
 import { isAbsolute, join, relative, resolve } from 'node:path';
 /** Content identity, including new files. Symlinks are identified, never followed. */
 export function workspaceFingerprint(directory) {
@@ -52,9 +52,14 @@ export function workspaceFingerprint(directory) {
             if (stat.isSymbolicLink())
                 hash.update(`link:${readlinkSync(full)}`);
             else if (stat.isFile()) {
-                if (stat.size > 64 * 1024 * 1024)
-                    throw new Error(`File too large to fingerprint safely: ${name}`);
-                hash.update(`file:${stat.mode}:${stat.size}:`).update(readFileSync(full));
+                hash.update(`file:${stat.mode}:${stat.size}:`);
+                const fd = openSync(full, 'r');
+                try {
+                    const block = Buffer.alloc(1024 * 1024);
+                    let bytes;
+                    while ((bytes = readSync(fd, block, 0, block.length, null)) > 0)
+                        hash.update(block.subarray(0, bytes));
+                } finally { closeSync(fd); }
             }
             else if (stat.isDirectory())
                 throw new Error(`Cannot safely fingerprint a nested repository or directory entry: ${name}`);
diff --git a/dist/code-intelligence/adapters/graphify.js b/dist/code-intelligence/adapters/graphify.js
index b0324f7..6ef0868 100644
--- a/dist/code-intelligence/adapters/graphify.js
+++ b/dist/code-intelligence/adapters/graphify.js
@@ -1,8 +1,8 @@
 import { McpBackendAdapter } from './mcp.js';
-export const GRAPHIFY_VERSION = '0.9.56';
+export const GRAPHIFY_VERSION = '0.9.55';
 export function createGraphifyAdapter(cwd, override) {
     return new McpBackendAdapter({
-        name: 'graphify', version: GRAPHIFY_VERSION, cwd, command: override?.command ?? 'python', args: override?.args ?? ['-m', 'graphify.serve'], framing: 'content-length', semantic: false, documents: true,
+        name: 'graphify', version: GRAPHIFY_VERSION, cwd, command: override?.command ?? 'python', args: override?.args ?? ['-m', 'graphify.serve'], framing: 'line', semantic: false, documents: true,
         aliases: { context: 'query_graph', symbol: 'get_node', neighbors: 'get_neighbors', trace: 'shortest_path', impact: 'get_pr_impact' },
     });
 }
diff --git a/dist/code-intelligence/adapters/serena.js b/dist/code-intelligence/adapters/serena.js
index 746f586..ffedacc 100644
--- a/dist/code-intelligence/adapters/serena.js
+++ b/dist/code-intelligence/adapters/serena.js
@@ -2,7 +2,7 @@ import { McpBackendAdapter } from './mcp.js';
 export const SERENA_VERSION = '1.7.1-dev';
 export function createSerenaAdapter(cwd, override) {
     return new McpBackendAdapter({
-        name: 'serena-lsp', version: SERENA_VERSION, cwd, command: override?.command ?? 'serena', args: override?.args ?? ['start-mcp-server', '--project-from-cwd', '--context', 'codex'], framing: 'content-length', semantic: true, documents: false,
+        name: 'serena-lsp', version: SERENA_VERSION, cwd, command: override?.command ?? 'serena', args: override?.args ?? ['start-mcp-server', '--project-from-cwd', '--context', 'codex'], framing: 'line', semantic: true, documents: false,
         aliases: {
             symbol: 'find_symbol', references: 'find_referencing_symbols', implementations: 'find_implementations', overview: 'get_symbols_overview', diagnostics: 'get_diagnostics_for_file',
             rename: 'rename_symbol', replace_symbol_body: 'replace_symbol_body', insert_before_symbol: 'insert_before_symbol', insert_after_symbol: 'insert_after_symbol', safe_delete: 'safe_delete_symbol',
diff --git a/dist/code-intelligence/adapters/mcp.js b/dist/code-intelligence/adapters/mcp.js
index 6a0fcb8..22a19e6 100644
--- a/dist/code-intelligence/adapters/mcp.js
+++ b/dist/code-intelligence/adapters/mcp.js
@@ -2,6 +2,10 @@ import { McpStdioClient } from '../mcp-client.js';
 function valueFromResult(value) {
     if (!value || typeof value !== 'object')
         return value;
+    // The pinned Serena server wraps JSON payloads in structuredContent.result.
+    if (value.structuredContent !== undefined && typeof value.structuredContent?.result === 'string') {
+        try { return JSON.parse(value.structuredContent.result); } catch { return value.structuredContent; }
+    }
     if (value.structuredContent !== undefined)
         return value.structuredContent;
     const text = Array.isArray(value.content) ? value.content.find((item) => item?.type === 'text')?.text : undefined;
diff --git a/dist/code-intelligence/mcp-server.js b/dist/code-intelligence/mcp-server.js
index e6ed6a6..d8a4392 100644
--- a/dist/code-intelligence/mcp-server.js
+++ b/dist/code-intelligence/mcp-server.js
@@ -11,7 +11,7 @@ const INPUT_SCHEMAS = {
     code_edit_apply: { type: 'object', required: ['workspace_id', 'plan_id', 'expected_snapshot_id', 'idempotency_key'], properties: { workspace_id: { type: 'string' }, plan_id: { type: 'string' }, expected_snapshot_id: { type: 'string' }, idempotency_key: { type: 'string' } } },
 };
 function arg(name, args) { return args.find(value => value.startsWith(`--${name}=`))?.slice(name.length + 3); }
-function summary(response) { return `status=${response.status} snapshot=${response.snapshot_id} warnings=${response.warnings.length} (full result in structuredContent)`; }
+function summary(response) { return JSON.stringify(response); }
 export async function runCodeIntelligenceServer(argv = process.argv.slice(2)) {
     const workspace = arg('workspace', argv) ?? process.cwd();
     const configured = loadConfig(workspace)?.codeIntelligence;
@@ -39,7 +39,7 @@ export async function runCodeIntelligenceServer(argv = process.argv.slice(2)) {
             return;
         }
         if (message.method === 'tools/list') {
-            send({ jsonrpc: '2.0', id: message.id, result: { tools: TOOL_DEFINITIONS.map(tool => ({ ...tool, inputSchema: INPUT_SCHEMAS[tool.name] })) } });
+            send({ jsonrpc: '2.0', id: message.id, result: { tools: TOOL_DEFINITIONS.map(tool => ({ ...tool, description: `${tool.description} Use workspace_id exactly: ${coordinator.workspace_id}.`, inputSchema: INPUT_SCHEMAS[tool.name] })) } });
             return;
         }
         if (message.method === 'tools/call') {
diff --git a/dist/loop/run-command.js b/dist/loop/run-command.js
index 5d70b65..8212fdc 100644
--- a/dist/loop/run-command.js
+++ b/dist/loop/run-command.js
@@ -549,6 +549,9 @@ export function runLoopCommand(targetDir, opts) {
             isolate,
             review,
             reporter,
+            // A plain independent review also needs a routed repair callback.
+            // The existing repair engine reruns every gate and review afterwards.
+            ...(review ? { repair: (context, request) => runner({ ...context, feedback: JSON.stringify(request) }) } : {}),
             ...(quality ?? {}),
             ...(quality ? { qualityEnabled: quality.qualityEnabled, qualityMetadata: quality.qualityMetadata } : {}),
         });
diff --git a/dist/quality/loop.js b/dist/quality/loop.js
index f4e55af..e681c12 100644
--- a/dist/quality/loop.js
+++ b/dist/quality/loop.js
@@ -70,7 +70,10 @@ export function runQualityRepairLoop(input) {
                     return { kind: 'blocked', reason: authorization.reason, summary: `${finding.message}${finding.evidence?.length ? `; evidence: ${finding.evidence.join(', ')}` : ''}`, repairs: usedRepairRounds };
                 usedRepairRounds = authorization.round;
                 emitStatus(authorization.round);
-                const repair = input.repair({ round: authorization.round, source: 'review', finding });
+                // Keep the primary finding while passing every actionable blocker
+                // from this exact verdict to the same routed repair round.
+                const findings = review.verdict.findings.filter(candidate => candidate.severity === 'blocking' && (candidate.actionable ?? true));
+                const repair = input.repair({ round: authorization.round, source: 'review', finding, findings });
                 if (repair.kind === 'blocked')
                     return { kind: 'blocked', reason: 'repair-failed', summary: repair.summary, repairs: usedRepairRounds };
                 const gates = input.rerunGates();
diff --git a/dist/loop/runner.js b/dist/loop/runner.js
index 3bd7e6c..59b4f4e 100644
--- a/dist/loop/runner.js
+++ b/dist/loop/runner.js
@@ -295,8 +295,17 @@ export function runReviewAgent(inv) {
         return { success: false, summary: processFailureSummary(error) };
     }
 }
+function feedbackForPrompt(feedback) {
+    try {
+        const request = JSON.parse(feedback);
+        // Structured independent review findings must reach the worker intact.
+        if (request?.source === 'review' && Array.isArray(request.findings))
+            return feedback;
+    } catch { /* ordinary diagnostic feedback keeps its original bound */ }
+    return feedback.slice(0, 8000);
+}
 export function makeAsyncRunner(agent, opts = {}) {
-    return (ctx) => startProviderProcess(agent, runnerInvocation(agent, buildClaudePrompt(ctx.story, contextBlockFor(ctx.targetDir, ctx.story) + (ctx.feedback ? "\nPrior independent failure; preserve useful existing changes and fix the root cause:\n" + ctx.feedback.slice(0, 8000) : ""), opts.onAmbiguity, opts.perfCommand), ctx.targetDir, true, opts.permissions ?? 'safe', opts.selection), { ...opts.process, attempt: ctx.attempt });
+    return (ctx) => startProviderProcess(agent, runnerInvocation(agent, buildClaudePrompt(ctx.story, contextBlockFor(ctx.targetDir, ctx.story) + (ctx.feedback ? "\nPrior independent failure; preserve useful existing changes and fix the root cause:\n" + feedbackForPrompt(ctx.feedback) : ""), opts.onAmbiguity, opts.perfCommand), ctx.targetDir, true, opts.permissions ?? 'safe', opts.selection), { ...opts.process, attempt: ctx.attempt });
 }
 export function makeRunner(agent, idleTimeoutMs = 0, opts = {}) {
     // Claude always streams (see runnerInvocation) — capture the stream so tokens are
@@ -307,7 +316,7 @@ export function makeRunner(agent, idleTimeoutMs = 0, opts = {}) {
         opts.onStart?.(agent, opts.selection ?? {});
         const started = Date.now();
         const attributed = (tokens) => tokens ? { ...tokens, provider: agent, role: 'parent', storyId: ctx.story.id, durationMs: Date.now() - started } : undefined;
-        const base = runnerInvocation(agent, buildClaudePrompt(ctx.story, contextBlockFor(ctx.targetDir, ctx.story) + (ctx.feedback ? "\nPrior independent failure; preserve useful existing changes and fix the root cause:\n" + ctx.feedback.slice(0, 8000) : ""), opts.onAmbiguity, opts.perfCommand), ctx.targetDir, captureTokens, opts.permissions ?? 'safe', opts.selection);
+        const base = runnerInvocation(agent, buildClaudePrompt(ctx.story, contextBlockFor(ctx.targetDir, ctx.story) + (ctx.feedback ? "\nPrior independent failure; preserve useful existing changes and fix the root cause:\n" + feedbackForPrompt(ctx.feedback) : ""), opts.onAmbiguity, opts.perfCommand), ctx.targetDir, captureTokens, opts.permissions ?? 'safe', opts.selection);
         const inv = buildWatchdogInvocation(base, idleTimeoutMs, ctx.targetDir, true);
         if (ctx.attempt)
             inv.args.splice(inv.args.indexOf('--'), 0, `--attempt=${ctx.attempt}`);
